# 开发约定

架构与模块接入只维护在 [README.md](README.md). 丢失上下文后先读它, 再按任务读相关源码, 不必通读项目. 用户最新明确要求优先.

- 保持代码和文档精简, 模型逐个实现; 不引入不必要的框架, 抽象或历史记录.
- 双击 HTML 离线运行; 模型用程序生成, 一类一个自包含文件, 文件夹入口亦遵循相同工厂接口.
- HTML 负责唯一主循环与通用能力, 场景负责配置, 模型负责外观/自身动画; 不加模型专用循环分支或跨模型依赖.
- 保留主人物, 碰撞, 射击, 换弹, 调试及模块容错等现有能力, 不因新增场景而隐式移除.
- 未获本次明确授权, 不运行游戏, 浏览器, 自动测试或截图; 可以静态检查与构建依赖. 交付如实说明验证范围.
- 项目自有注释用中文和英文半角标点; 依赖产物不手改, 保留上游许可证.
- 新源文件及时 `git add`, 本地产物放忽略目录; 交付前检查状态, 不遗留应跟踪文件.
- 提交格式 `type: 简洁中文说明.`, 前缀无括号, 英文标点. 不改写他人提交或强推, 未要求不推送.
