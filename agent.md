# 项目需求与开发规范

本文件是后续开发的项目约束. 用户最新明确指令优先, 不应重复询问已经授权的工作.

## 1. 产品与交付要求

- 使用 Three.js 实现第一人称网页射击游戏, 可以使用其他库, 但优先减少代码和依赖.
- 保留整个项目文件夹后, 双击 `index.html` 即可离线运行. 玩家不需要安装依赖, 启动服务器或执行构建命令.
- 使用本地普通脚本加载, 不依赖在线 CDN, 运行时 ES Modules 或通过 fetch 读取本地 JSON.
- 主程序是一个 HTML 文件, 不是将所有资源打包进一个 HTML. 本地依赖, 模型和布局随项目交付.
- 保持低代码量和可读性, 通过简化系统与复用能力减少代码, 不使用混淆或手工压缩代替结构设计.

## 2. 主程序与模型职责

- `index.html` 必须实现唯一主循环, 玩家移动与射击, 重力与碰撞, 通用命中与伤害, 光影效果和调试模式.
- 界面, 音效, 模块加载和异常处理也由主程序管理.
- 所有模型通过程序生成, 模型几何构造不得放进 HTML.
- 每种模型一个文件, 同种模型的多个实例复用工厂. 不将所有模型集中到一个大文件.
- 模型之间不建立运行时依赖. 修改一个模型时, 应尽量只需阅读主程序接口和该模型文件.
- 新增实例或修改摆放时, 另行修改 `scene-layout.js`.
- `models/enemy.js` 单独承载敌人造型, 关节动画, 状态机和移动/射击决策, 避免将敌人细节塞入主程序.
- 敌人调用主程序的通用移动和射击接口. 模型不自行创建主循环或定时器.
- 工厂注册约定为 `FPS.models[name](THREE, options)`, 返回 `{ root }` 及可选回调. 接口细节见 `README.md`.

## 3. 独立场景布局

- `scene-layout.js` 是初始场景实例的唯一布局来源.
- 布局包括模型文件目录, 实例 ID, 模型类型, 位置, 旋转, 缩放, 碰撞开关及尺寸, 初始状态和模型参数.
- 出生点, 相机挂载的武器, 灯光和环境配置也记录在布局中. 灯光的创建和控制仍在 HTML 中实现.
- 初始元素由布局声明. 枪口火焰和曳光等临时实例由主程序创建, 模型程序仍保留在各自文件中.
- 距离单位为米, Y 轴向上, 欧拉角为弧度, 顺序为 XYZ.
- 碰撞尺寸是局部尺寸, 与模型统一应用根节点变换, 不重复缩放.
- 简单碰撞使用 `collision.size` 和 `collision.offset`, 复杂外形使用 `collision.boxes` 声明多个局部盒子.
- 掩体碰撞按底座, 墙身和顶盖分层. 集装箱碰撞包含外壳及结构边框.
- 当前静态碰撞使用世界轴对齐盒子. 非直角旋转可能保守扩大范围, 不应声称它是精确的旋转碰撞体.
- 玩家水平截面半径为 0.32 米. 相机中心与墙面保留身体半径的距离属于正常碰撞行为.
- 撞墙时细分最后一步到接触位置, 避免整步撤回产生额外间隙. 调试线框显示实际参与碰撞的盒子.

## 4. 玩法与操作

- 玩家使用第一人称视角移动和射击. 正常模式具有重力, 跳跃和障碍碰撞.
- 敌人必须能够移动和开枪, 不能仅作为静态靶子.
- 敌人需要关节动画, 即使四肢由简单几何体组成, 也应体现行走和持枪动作.
- 当前训练场包含 6 名敌人, 一把步枪, 掩体和雷达, 支持连续射击, 换弹, 命中反馈, 死亡, 完成训练及重新部署.
- 敌人使用程序化行走, 后坐力和倒地动画, 使用简单避障. 当前不保证复杂迷宫通行, 也不实现敌人之间的物理推挤.
- 玩家和敌人复用射线命中机制. 墙体遮挡射击, 头部命中造成双倍伤害.
- WASD 移动, 鼠标观察, 左键射击, R 换弹, Space 跳跃, Shift 奔跑, Esc 暂停.

## 5. 调试模式

- 使用 B 键开关调试模式, 不使用 F2/F3, 避免与浏览器功能键冲突.
- 开启时自动显示绿色碰撞线框, 退出时隐藏, 不需要另一个线框开关.
- 调试模式无重力, 可穿墙, 免伤, 停止双方开火, 敌人仍可移动.
- W/S 沿镜头完整朝向移动, 必须包含上下俯仰分量. Space/Ctrl 沿世界竖直方向升降, Shift 加速.
- 显示帧率, 坐标, 朝向, 模块状态和碰撞线框.
- 退出时检查实际场景碰撞与下方支撑, 防止卡墙或从场地外掉落. 无可用碰撞体时保持自由飞行.

## 6. 模块容错

- 单个模型缺失, 加载失败, 语法错误, 未注册, 构造失败或回调异常, 不应中断其他可用功能.
- 错误在页面显示, 尽量包含文件或实例标识和原因, 不能只输出到控制台.
- 重复错误合并显示, 避免逐帧刷屏.
- 失败实例不保留隐形碰撞. 更新失败的角色被停用并从任务目标数量中排除, 失败武器不能在恢复游戏时重新启用.
- 布局部分无效时跳过对应条目. 布局整体失败时保留空场景与无重力观察状态.
- 特效缺失不影响射击和伤害判定. 不在 HTML 中内置兜底模型.
- Three.js 缺失或 WebGL 不可用时保留错误界面和重新加载入口, 此时无法继续 3D 渲染.
- 同页面脚本死循环, 内存耗尽或恶意破坏共享变量, 不属于可恢复异常隔离的保证范围.

## 7. 工作方式与验证边界

- 用户已经授权实现游戏和整理仓库, 不需要再次询问是否开始.
- 用户明确要求由本人打开 `index.html` 进行实际运行验证, 以节省时间.
- 未获得新的明确授权时, 助手不要运行游戏, 浏览器, 自动测试或截图.
- 助手可以阅读源码, 进行静态审查, 修正问题, 整理说明, 检查 Git 差异和管理仓库.
- 保留的 `tests/game.mjs` 和开发构建工具不参与玩家启动流程, 不自动执行.
- 首版曾通过 18 项离线 file:// 集成检查并检查截图, 这只是历史记录, 不代表后续修改已运行验证.
- 交付时如未运行测试, 必须如实说明, 不能承诺运行时完全无缺陷.

## 8. 注释规范

- 自行编写和修改的代码注释必须使用中文说明, 必要的标识符, API 名称和路径保留原样.
- 注释中的所有标点符号必须使用英文半角标点, 不得出现中文标点.
- 使用英文逗号, 冒号, 分号, 括号, 引号和句号, 例如 `// 更新镜头方向, 避免射击使用上一帧的数据.`.
- 注释解释职责, 原因, 接口约定和重要限制, 避免重复描述显而易见的代码.
- HTML, JavaScript, 开发脚本和配置文件的项目自有注释均遵守此规范.
- 第三方许可证和生成依赖保持上游原文, 不手工翻译或改写 `vendor/` 中的内容.

## 9. Git 与提交规范

- 远程仓库为 `git@github.com:tivvvv/web-cs.git`, 远程名称为 `origin`, 本地主分支为 `main`.
- 创建项目源文件后及时加入 Git. 提交前检查差异与 `git status`, 不遗留应跟踪的项目文件.
- 忽略本地依赖, 测试产物, 日志, 凭据配置和编辑器临时文件. 不忽略离线运行必需的 `vendor/` 依赖.
- 提交消息以 type 开头, type 后使用英文冒号和一个空格, 后接简洁, 明确, 专业的中文描述, 最后以英文句号结尾.
- 固定格式为 `type: 简洁中文说明.`.
- type 使用 `feat`, `fix`, `refactor`, `docs`, `chore` 等不带括号的前缀, 不添加括号包围的 scope.
- 提交消息中的全部标点必须为英文半角标点, 不得出现中文标点.
- 示例为 `feat: 实现离线射击训练场与调试模式.` 或 `fix: 修正掩体碰撞范围与贴边移动.`.
- 不改写他人提交, 不强制推送. 关联远程地址不等同于要求推送代码, 推送按用户明确指令执行.

## 10. 当前目录

```text
index.html              主程序和页面
scene-layout.js         场景布局与参数
models/                 每种模型一个文件
vendor/                 已构建的离线依赖和许可证
tools/build-vendor.mjs   开发阶段依赖构建工具
tests/game.mjs          保留的手动集成检查工具
README.md               运行说明和模型接口
agent.md                需求与开发规范
```
